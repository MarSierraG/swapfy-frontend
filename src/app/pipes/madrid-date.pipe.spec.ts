import { MadridDatePipe } from './madrid-date.pipe';

describe('MadridDatePipe', () => {
  it('create an instance', () => {
    const pipe = new MadridDatePipe();
    expect(pipe).toBeTruthy();
  });
});
